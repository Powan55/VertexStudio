# Copy to .env (which is git-ignored) and fill per client.
# PUBLIC_ vars are exposed to the browser by Astro — only put non-secret,
# public-by-design values here. The Web3Forms access key is a public submit
# key (rate-limited server-side), so PUBLIC_ is correct.

PUBLIC_WEB3FORMS_KEY=your-web3forms-access-key
